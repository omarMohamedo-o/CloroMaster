# ==========================================
# Backend API Deployment with Auto-scaling
# ==========================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: chloromaster
  labels:
    app: backend
    tier: api
    version: v1
spec:
  replicas: 2 # Start with 2 replicas for production (HPA will adjust)
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: backend
      tier: api
  template:
    metadata:
      labels:
        app: backend
        tier: api
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5000"
        prometheus.io/path: "/metrics"
    spec:
      # HostAliases removed: rely on DNS in cluster. If you previously used
      # hostAliases to map 'mailhog' for local testing, keep a local overlay.

      # Security Context
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault

      # Init Container for Database Migration
      # NOTE: Disabled until migration scripts are added to Docker image
      # initContainers:
      #   - name: db-migration
      #     image: chloromaster/backend:latest
      #     imagePullPolicy: IfNotPresent
      #     command: ["/bin/bash", "-c"]
      #     args:
      #       - |
      #         set -e
      #         echo "=========================================="
      #         echo "Starting Database Migration..."
      #         echo "=========================================="
      #
      #         # Run migration script
      #         /app/scripts/migrate.sh
      #
      #         # Seed initial data (admin user)
      #         /app/scripts/seed.sh
      #
      #         echo "=========================================="
      #         echo "Migration Complete!"
      #         echo "=========================================="
      #     volumeMounts:
      #       - name: backend-data
      #         mountPath: /app/data
      #     env:
      #       - name: DB_PATH
      #         value: /app/data/chloromaster.db
      #     envFrom:
      #       - configMapRef:
      #           name: chloromaster-config
      #       - secretRef:
      #           name: chloromaster-secrets
      #     securityContext:
      #       runAsNonRoot: true
      #       runAsUser: 1001
      #       allowPrivilegeEscalation: false

      containers:
        - name: backend
          image: chloromaster/backend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 5000
              protocol: TCP

          # Environment Variables
          envFrom:
            - configMapRef:
                name: chloromaster-config
            - secretRef:
                name: chloromaster-secrets
          env:
            # Force IPv4 for DNS resolution (mitigate .NET DNS issues in containers)
            - name: DOTNET_SYSTEM_NET_DISABLEIPV6
              value: "true"
            - name: DOTNET_SYSTEM_NET_HTTP_SOCKETSHTTPHANDLER_HTTP2SUPPORT
              value: "false"
            # Fix for .NET async DNS resolution EAGAIN error in Kubernetes
            - name: DOTNET_SYSTEM_NET_SOCKETS_INLINE_COMPLETIONS
              value: "1"
            - name: DOTNET_SYSTEM_NET_SOCKETS_THREAD_COUNT
              value: "50"
            # Disable dev-only fallback in deployments; require valid emails
            - name: ALLOW_DATASHEET_WITHOUT_EMAIL
              value: "false"
            - name: Smtp__Host
              valueFrom:
                secretKeyRef:
                  name: chloromaster-secrets
                  key: Smtp__Host
            - name: Smtp__Port
              valueFrom:
                secretKeyRef:
                  name: chloromaster-secrets
                  key: Smtp__Port
            - name: Smtp__Username
              valueFrom:
                secretKeyRef:
                  name: chloromaster-secrets
                  key: Smtp__Username
            - name: Smtp__Password
              valueFrom:
                secretKeyRef:
                  name: chloromaster-secrets
                  key: Smtp__Password
            - name: Smtp__UseSsl
              valueFrom:
                secretKeyRef:
                  name: chloromaster-secrets
                  key: Smtp__UseSsl
            - name: Smtp__FromEmail
              valueFrom:
                secretKeyRef:
                  name: chloromaster-secrets
                  key: Smtp__FromEmail
            - name: Smtp__FromName
              valueFrom:
                secretKeyRef:
                  name: chloromaster-secrets
                  key: Smtp__FromName
            - name: Smtp__AdminEmail
              valueFrom:
                secretKeyRef:
                  name: chloromaster-secrets
                  key: Smtp__AdminEmail
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: chloromaster-secrets
                  key: CONNECTION_STRING
            - name: Security__PasswordSalt
              valueFrom:
                secretKeyRef:
                  name: chloromaster-secrets
                  key: PASSWORD_SALT

          # Volume Mounts
          volumeMounts:
            - name: backend-data
              mountPath: /app/data
            - name: backend-logs
              mountPath: /app/logs

          # Resource Limits and Requests
          resources:
            requests:
              cpu: 500m # 0.5 CPU
              memory: 512Mi
            limits:
              cpu: 1 # 1 CPU max per pod
              memory: 1Gi

          # Probes
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3

          startupProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 0
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 30

          # Security Context
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            runAsNonRoot: true
            runAsUser: 1001
            capabilities:
              drop:
                - ALL
              add:
                - NET_BIND_SERVICE

      # Volumes
      volumes:
        - name: backend-data
          persistentVolumeClaim:
            claimName: backend-data-pvc
        - name: backend-logs
          persistentVolumeClaim:
            claimName: backend-logs-pvc

      # Affinity Rules (spread across nodes)
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - backend
                topologyKey: kubernetes.io/hostname
