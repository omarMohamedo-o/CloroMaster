# ChloroMaster Kubernetes Architecture

## 🏗️ High-Level Architecture

```
                            ┌─────────────────────────┐
                            │   Internet / Users      │
                            └────────────┬────────────┘
                                        │
                                        │ HTTPS (443)
                                        ▼
                            ┌─────────────────────────┐
                            │  Ingress Controller     │
                            │  (Nginx + SSL/TLS)      │
                            │  - Rate Limiting        │
                            │  - Load Balancing       │
                            │  - Security Headers     │
                            └────────────┬────────────┘
                                        │
                    ┌──────────────────┴──────────────────┐
                    │                                      │
                    ▼                                      ▼
        ┌──────────────────────┐              ┌──────────────────────┐
        │  Frontend Service    │              │  Backend Service     │
        │  (ClusterIP)         │              │  (ClusterIP)         │
        │  Port: 3000          │              │  Port: 5000          │
        └──────────┬───────────┘              └──────────┬───────────┘
                   │                                      │
        ┌──────────┴──────────┐                ┌─────────┴──────────┐
        │                     │                │                    │
        ▼                     ▼                ▼                    ▼
  ┌─────────┐         ┌─────────┐      ┌─────────┐          ┌─────────┐
  │Frontend │         │Frontend │      │Backend  │          │Backend  │
  │  Pod 1  │   ...   │  Pod N  │      │  Pod 1  │   ...    │  Pod N  │
  │ (React) │         │ (React) │      │ (.NET)  │          │ (.NET)  │
  └─────────┘         └─────────┘      └────┬────┘          └────┬────┘
                                             │                     │
  3-15 Replicas                              │                     │
  Auto-scaled                                │                     │
                                             └──────────┬──────────┘
                                                        │
                                                        ▼
                                            ┌─────────────────────┐
                                            │ Persistent Storage  │
                                            │  - Database (5Gi)   │
                                            │  - Logs (2Gi)       │
                                            └─────────────────────┘

                                            2-10 Replicas
                                            Auto-scaled
```

## 🔄 Auto-scaling Flow

```
┌──────────────────────────────────────────────────────────────┐
│                    Metrics Server                             │
│              (Collects CPU & Memory metrics)                  │
└───────────────────┬──────────────────────────────────────────┘
                    │
                    │ Metrics
                    ▼
┌──────────────────────────────────────────────────────────────┐
│           Horizontal Pod Autoscaler (HPA)                     │
│                                                               │
│  Frontend HPA:                    Backend HPA:                │
│  ├─ Min: 3 pods                  ├─ Min: 2 pods              │
│  ├─ Max: 15 pods                 ├─ Max: 10 pods             │
│  ├─ Target: 60% CPU              ├─ Target: 70% CPU          │
│  └─ Target: 70% Memory           └─ Target: 80% Memory       │
│                                                               │
│  Scale Up: Fast (30s-60s)                                     │
│  Scale Down: Slow (5min stabilization)                        │
└───────────────────┬──────────────────────────────────────────┘
                    │
                    │ Scale Commands
                    ▼
┌──────────────────────────────────────────────────────────────┐
│                      Deployments                              │
│  ├─ Adjusts replica count                                     │
│  ├─ Creates/deletes pods                                      │
│  └─ Maintains desired state                                   │
└──────────────────────────────────────────────────────────────┘
```

## 🛡️ Security Layers

```
┌────────────────────────────────────────────────────────────┐
│ Layer 1: Network Ingress                                    │
│ ├─ SSL/TLS Termination                                      │
│ ├─ Rate Limiting (20 req/s)                                 │
│ ├─ DDoS Protection                                          │
│ └─ Security Headers (XSS, CSRF, etc.)                       │
└────────────────┬───────────────────────────────────────────┘
                 │
┌────────────────▼───────────────────────────────────────────┐
│ Layer 2: Network Policies                                   │
│ ├─ Pod-to-Pod isolation                                     │
│ ├─ Ingress rules (frontend ↔ backend)                      │
│ ├─ Egress rules (DNS, HTTPS only)                          │
│ └─ Namespace isolation                                      │
└────────────────┬───────────────────────────────────────────┘
                 │
┌────────────────▼───────────────────────────────────────────┐
│ Layer 3: Container Security                                 │
│ ├─ Non-root users (UIDs: 101, 1001)                        │
│ ├─ Read-only root filesystem                                │
│ ├─ Dropped capabilities (ALL)                               │
│ ├─ Added capabilities (NET_BIND_SERVICE only)              │
│ └─ Security profiles (seccomp)                              │
└────────────────┬───────────────────────────────────────────┘
                 │
┌────────────────▼───────────────────────────────────────────┐
│ Layer 4: Application Security                               │
│ ├─ Secret management (encrypted)                            │
│ ├─ JWT token authentication                                 │
│ ├─ Password hashing (SHA256)                                │
│ └─ API input validation                                     │
└──────────────────────────────────────────────────────────────┘
```

## 📊 Traffic Flow

```
User Request
     │
     ├─→ DNS Resolution (chloromaster.com)
     │
     ├─→ Ingress Controller (Port 80/443)
     │   ├─ SSL Termination
     │   ├─ Rate Limiting Check
     │   └─ Route Selection
     │
     ├─→ Service (ClusterIP)
     │   └─ Load Balancing
     │
     ├─→ Pod Selection (Round Robin)
     │   ├─ Health Check (Readiness Probe)
     │   └─ Session Affinity (if enabled)
     │
     └─→ Container
         ├─ Application Processing
         └─ Database Access (if backend)

Response
     │
     ├─← Container
     ├─← Pod
     ├─← Service
     ├─← Ingress (+ Security Headers)
     └─← User
```

## 🔄 Deployment & Update Flow

```
Developer
     │
     ├─→ Build Docker Images
     │   ├─ Frontend: node build → nginx
     │   └─ Backend: dotnet publish
     │
     ├─→ Tag & Push to Registry
     │
     ├─→ Update Kubernetes Deployment
     │
     └─→ Rolling Update Strategy
         │
         ├─→ Create new ReplicaSet
         │
         ├─→ Scale up new pods (1 by 1)
         │   ├─ Wait for pod ready
         │   ├─ Health check pass
         │   └─ Add to service endpoints
         │
         ├─→ Scale down old pods (1 by 1)
         │   ├─ Remove from service
         │   ├─ Wait for connections drain
         │   └─ Terminate pod
         │
         └─→ Monitor rollout status
             ├─ Success → Complete
             └─ Failure → Auto rollback
```

## 💾 Data Persistence

```
┌────────────────────────────────────────────────┐
│         PersistentVolumeClaim (PVC)            │
│  ├─ backend-data-pvc (5Gi)                     │
│  └─ backend-logs-pvc (2Gi)                     │
└───────────────────┬────────────────────────────┘
                    │
                    ▼
┌────────────────────────────────────────────────┐
│           Storage Provisioner                   │
│  (Dynamic volume provisioning)                  │
└───────────────────┬────────────────────────────┘
                    │
                    ▼
┌────────────────────────────────────────────────┐
│         PersistentVolume (PV)                   │
│  └─ Actual storage on disk/cloud               │
└───────────────────┬────────────────────────────┘
                    │
                    ▼
        ┌───────────────────────┐
        │     Backend Pod       │
        │  /app/data → DB       │
        │  /app/logs → Logs     │
        └───────────────────────┘
```

## 🎯 Resource Management

```
Namespace: chloromaster
│
├─ Resource Quota (Namespace-wide)
│  ├─ CPU: 10-20 cores
│  ├─ Memory: 20-40Gi
│  ├─ Storage: 50Gi
│  └─ Pods: 50 max
│
├─ Limit Range (Per Container)
│  ├─ CPU: 50m-2 cores
│  ├─ Memory: 64Mi-2Gi
│  └─ Storage: 1-10Gi per PVC
│
├─ Frontend Pods
│  ├─ Request: 100m CPU, 128Mi
│  └─ Limit: 200m CPU, 256Mi
│
└─ Backend Pods
   ├─ Request: 250m CPU, 256Mi
   └─ Limit: 500m CPU, 512Mi
```

## 🔍 Monitoring & Observability

```
┌────────────────────────────────────────────────┐
│            Metrics Collection                   │
│  ├─ Metrics Server (CPU, Memory)               │
│  ├─ cAdvisor (Container metrics)               │
│  └─ kubelet (Node metrics)                     │
└───────────────────┬────────────────────────────┘
                    │
                    ▼
┌────────────────────────────────────────────────┐
│          Metrics Storage & Query                │
│  └─ Prometheus (optional)                       │
└───────────────────┬────────────────────────────┘
                    │
                    ▼
┌────────────────────────────────────────────────┐
│            Visualization                        │
│  ├─ kubectl top (CLI)                          │
│  ├─ Kubernetes Dashboard (Web UI)              │
│  └─ Grafana (optional)                         │
└────────────────────────────────────────────────┘
```

## 🚀 High Availability Setup

```
Multiple Nodes
│
├─ Node 1
│  ├─ Frontend Pod 1
│  └─ Backend Pod 1
│
├─ Node 2
│  ├─ Frontend Pod 2
│  └─ Backend Pod 2
│
└─ Node 3
   ├─ Frontend Pod 3
   └─ Backend Pod 3

Features:
├─ Pod Anti-Affinity (spread across nodes)
├─ Pod Disruption Budgets (min available)
├─ Multiple Replicas (always 2+ backend, 3+ frontend)
├─ Health Probes (auto-restart unhealthy pods)
└─ Rolling Updates (zero-downtime deployments)
```

---

**For detailed implementation**: See `KUBERNETES_GUIDE.md`
**For quick commands**: See `k8s/QUICK_REFERENCE.md`
